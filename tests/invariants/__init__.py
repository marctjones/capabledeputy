"""003 invariant tests (T015 scaffold).

Tests in this package are gated by `@pytest.mark.invariant`. They
encode security-model invariants from the spec (Principle III:
test-first, invariants as tests). They are part of the standard
test suite — `pytest` without args runs them.
"""
