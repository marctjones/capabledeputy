"""Upstream MCP server integration.

Wraps subprocess MCP servers (Filesystem, Fetch, Gmail, etc.) with
CapableDeputy's policy + label enforcement. The same security
guarantees that apply to native tools apply to upstream MCP tools.
"""
