SPECIFICATION Spec

CONSTANTS
    Sessions = {s1, s2}
    Targets = {alice, bob, pharmacy, file1}
    Tools = {memread, mempurchase, sendmail}
    MaxTurns = 4

INVARIANTS
    Inv

CHECK_DEADLOCK FALSE
