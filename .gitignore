# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info/

# Virtual environments
.venv/
venv/
env/

# Testing & coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.json
coverage.xml
*.cover

# Type checkers / linters
.mypy_cache/
.pyright_cache/
.ruff_cache/

# IDE
.vscode/
.idea/
*.swp
*.swo

# uv (uv.lock is committed)
.uv-cache/

# SwiftPM
.build/

# OS
.DS_Store
Thumbs.db

# CapableDeputy runtime
state.db
audit.jsonl
*.sock

# Agent-local config (machine-specific; speckit skills + local settings)
.claude/

# Secrets
*.key
*.KEY
*.pem
.env
.env.*
CLAUDEAPI.*
client_secret*.json

# Operator-local runtime configs (machine-specific; reference out-of-repo
# secret paths but contain no secrets themselves). The "-local" suffix is
# the convention; do NOT commit these.
configs/*-local.yaml
configs/*-local.yml
configs/*-local.json
